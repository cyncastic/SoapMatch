%h1 Listing notes

%table
  %thead
    %tr
      %th Name
      %th Note category
      %th
      %th
      %th

  %tbody
    - @notes.each do |note|
      %tr
        %td= note.name
        %td= note.note_category.name
        %td= link_to 'Show', note
        %td= link_to 'Edit', edit_note_path(note)
        %td= link_to 'Delete', note, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Note', new_note_path
